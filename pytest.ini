[pytest]
# The app is a client of the engine, so both source trees are importable and both
# suites run from the repository root with a single command.
pythonpath = . engine/src
testpaths = app/tests engine/tests
addopts = --strict-markers
markers =
    slow: marks tests as slow
